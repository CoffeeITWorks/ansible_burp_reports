---
driver:
  name: docker

docker:
  containers:

      - name: ansible_burp2_reports-01
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1

      - name: ansible_burp2_reports-02
        image: ubuntu
        image_version: '14.04'
        ansible_groups:
          - group1

      # In docker cannot be tested with centos 7 due to https://github.com/ansible/ansible-modules-core/issues/593
      - name: ansible_burp2_reports-03
        image: centos
        image_version: 7
        ansible_groups:
          - group1

verifier:
  name: testinfra
